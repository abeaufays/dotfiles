Before starting a task, run `git --git-dir=$HOME/dotfiles.git/ --work-tree=$HOME ls-tree -r --name-only HEAD` to get a sense of what my config contains. (`dgit` is an alias containing referring the worktree) 

There is also a ~/.zshrc available.
